import type { Metadata } from "next";
import Link from "next/link";
import { platformItems } from "../_data/platform";

export const metadata: Metadata = { title: "Hands-on labs", description: "Small, reproducible cloud and DevOps portfolio labs." };
export default function LabsPage(){const labs=platformItems.filter(item=>item.type==="Lab");return <div className="platform-page"><div className="page-kicker">LABS / 03</div><h1 className="page-title">Build something small.</h1><p className="page-lede">Focused exercises with a visible result. Each lab is labelled as a new portfolio demonstration and designed to leave a reproducible trail.</p><div className="project-list">{labs.map(lab=><article className="project-card" key={lab.slug}><div className="project-card-top"><span className="type-badge">Portfolio lab</span><span>{lab.level} · {lab.time}</span></div><h2>{lab.title}</h2><p>{lab.description}</p><div className="tag-row">{lab.topics.map(topic=><span key={topic}>{topic}</span>)}</div><Link className="card-link" href={lab.href}>Open the lab plan ↗</Link></article>)}</div><div className="callout"><span>Lab rule</span><p>Keep the scope small enough to finish, inspect, explain, and clean up in one sitting.</p></div></div>}
