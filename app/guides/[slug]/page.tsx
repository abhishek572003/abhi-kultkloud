import type { Metadata } from "next";
import Link from "next/link";
import { platformItems } from "../../_data/platform";

export function generateStaticParams(){return platformItems.filter(item=>item.type==="Guide").map(item=>({slug:item.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params; const item=platformItems.find(value=>value.slug===slug); return {title:item?.title??"Guide",description:item?.description};}
export default async function GuidePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const item=platformItems.find(value=>value.slug===slug); if(!item) return <div className="platform-page"><h1 className="page-title">Guide not found</h1><Link className="card-link" href="/search">Return to search ↗</Link></div>; return <article className="article-page"><div className="article-top"><span className="type-badge">{item.type}</span><span>{item.level} · {item.time}</span></div><h1 className="page-title">{item.title}</h1><p className="article-lede">{item.description}</p><div className="article-layout"><aside><p className="page-kicker">ON THIS PAGE</p><a href="#idea">The idea</a><a href="#practice">Practice</a><a href="#next">Next step</a></aside><div className="article-body"><section id="idea"><p className="page-kicker">THE IDEA</p><h2>Start with the system, then choose the tool.</h2><p>This guide is a working note: it explains the mental model, uses a small example, and leaves you with a concrete next step. The goal is durable understanding you can carry between platforms.</p><div className="code-block"><span>terminal</span><pre>{`$ learn --topic ${item.topics[0].toLowerCase()}\n→ identify the problem\n→ choose the smallest useful primitive\n→ observe the result`}</pre></div></section><section id="practice"><p className="page-kicker">PRACTICE</p><h2>Make the concept observable.</h2><p>Write down the inputs, expected outcome, and one failure mode before you run the exercise. When the result differs, that difference is the material you need to understand.</p><ul><li>Define the boundary of the system.</li><li>Use one small reproducible example.</li><li>Record the decision and its tradeoff.</li></ul></section><section id="next"><p className="page-kicker">NEXT STEP</p><h2>Move from reading to doing.</h2><p>Continue with the related labs and projects in the platform library.</p><Link className="button button-primary" href="/labs">Browse hands-on labs ↗</Link></section></div></div></article>;}
