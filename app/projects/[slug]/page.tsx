import type { Metadata } from "next";
import Link from "next/link";
import { projects } from "../../_data/projects";

export function generateStaticParams(){return projects.map(item=>({slug:item.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const item=projects.find(value=>value.slug===slug);return {title:item?.title??"Project",description:item?.description};}
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=projects.find(value=>value.slug===slug);if(!item)return <div className="platform-page"><h1 className="page-title">Project not found</h1><Link className="card-link" href="/projects">Return to projects ↗</Link></div>;return <article className="article-page"><div className="article-top"><span className="type-badge">{item.status}</span><span>{item.level} · {item.time}</span></div><h1 className="page-title">{item.title}</h1><p className="article-lede">{item.description}</p><div className="tag-row">{item.topics.map(topic=><span key={topic}>{topic}</span>)}</div><div className="article-layout"><aside><p className="page-kicker">PROJECT NOTES</p><a href="#problem">Problem</a><a href="#architecture">Architecture</a><a href="#lessons">Lessons</a></aside><div className="article-body"><section id="problem"><p className="page-kicker">01 / PROBLEM</p><h2>Start with the boundary worth explaining.</h2><p>{item.problem}</p></section><section id="architecture"><p className="page-kicker">02 / ARCHITECTURE</p><h2>Keep the moving parts visible.</h2><div className="code-block"><span>architecture sketch</span><pre>{item.architecture}</pre></div><p>Keep the architecture diagram close to the decision it explains: what is managed, what is configured, and where the system can fail.</p></section><section id="lessons"><p className="page-kicker">03 / LESSONS</p><h2>Document the tradeoffs.</h2><ul>{item.lessons.map(lesson=><li key={lesson}>{lesson}</li>)}</ul><Link className="button button-primary" href="/labs">Find a related lab ↗</Link></section></div></div></article>;}
