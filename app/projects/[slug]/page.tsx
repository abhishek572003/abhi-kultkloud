import type { Metadata } from "next";
import Link from "next/link";
import { platformItems } from "../../_data/platform";

export function generateStaticParams(){return platformItems.filter(item=>item.type==="Project").map(item=>({slug:item.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const item=platformItems.find(value=>value.slug===slug);return {title:item?.title??"Project",description:item?.description};}
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=platformItems.find(value=>value.slug===slug);if(!item)return <div className="platform-page"><h1 className="page-title">Project not found</h1><Link className="card-link" href="/projects">Return to projects ↗</Link></div>;return <article className="article-page"><div className="article-top"><span className="type-badge">{item.type}</span><span>{item.level} · {item.time}</span></div><h1 className="page-title">{item.title}</h1><p className="article-lede">{item.description}</p><div className="article-layout"><aside><p className="page-kicker">PROJECT NOTES</p><a href="#problem">Problem</a><a href="#architecture">Architecture</a><a href="#lessons">Lessons</a></aside><div className="article-body"><section id="problem"><p className="page-kicker">01 / PROBLEM</p><h2>Make the request path easy to reason about.</h2><p>The project is a compact learning implementation. It is intentionally small enough to inspect end to end while still showing how identity, APIs, compute, and persistence fit together.</p></section><section id="architecture"><p className="page-kicker">02 / ARCHITECTURE</p><h2>Trace the boundary between managed services.</h2><div className="code-block"><span>request flow</span><pre>{`browser → authenticated API → function → managed data\n              ↓\n          identity + least privilege`}</pre></div><p>Keep the architecture diagram close to the decision it explains: what is managed, what is configured, and where the system can fail.</p></section><section id="lessons"><p className="page-kicker">03 / LESSONS</p><h2>Document the tradeoffs.</h2><ul><li>Managed services reduce operational work but increase the need for clear boundaries.</li><li>Least privilege is an architecture decision, not a final checklist.</li><li>Repeatable setup is easier to review when configuration lives beside the explanation.</li></ul><Link className="button button-primary" href="/labs">Find a related lab ↗</Link></section></div></div></article>;}
